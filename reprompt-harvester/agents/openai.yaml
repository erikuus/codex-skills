interface:
  display_name: "Reprompt Harvester"
  short_description: "Harvest finished threads into DEVLOG.md"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.png"
  default_prompt: "Use $reprompt-harvester to append this finished thread to the target project's DEVLOG.md, creating DEVLOG.md if missing, inferring a live-thread label when needed, and appending only one complete entry block at a time."
